using System;
using UnityEngine;

public class Commands : MonoBehaviour
{
    [SerializeField] private SelectionManager selectionManager;
    private void Update()
    {
        if (Input.GetKey("q"))
        {
            
        }
        
        
    }
}